galaxy_info:
  author: smlloyd
  role_name: sssd_ad
  description: Configure sssd for Active Directory
  license: "Beerware License"
  min_ansible_version: 2.7
  platforms:
    - name: EL
      versions:
        - 7
        - 8
    - name: Fedora
      versions:
        - 29
        - 30
        - 31
  galaxy_tags: []
dependencies:
  - src: smlloyd.authselect
    when:
    - ansible_os_family == 'RedHat'
    - ansible_distribution_major_version|int >= 8
